import styled from 'styled-components'

export const StyledNotFoundPage = styled.div`
	.page__title {
		margin-top: 0;
		font-size: 2.5em;
		user-select: none;
	}

	.page__image {
		width: 300px;
		user-select: none;
		pointer-events: none;
	}
`
