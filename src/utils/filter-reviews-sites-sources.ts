import { Review } from '@entities/review.types'
import { SiteSource } from '@entities/site-sources.types'

export const filterReviewsSitesSources = (reviews: Review[], sources: SiteSource[]) => {
	return reviews
}
